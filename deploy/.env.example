# Copy to deploy/.env and fill in real values. Never commit deploy/.env.
#
# Generate JWT_SECRET:          openssl rand -base64 48
# Generate BANK_DETAILS_DEK_HEX: openssl rand -hex 32
# Generate SEED_SETUP_TOKEN:    openssl rand -hex 24

POSTGRES_PASSWORD=
JWT_SECRET=
BANK_DETAILS_DEK_HEX=
SEED_SETUP_TOKEN=
PUBLIC_DOMAIN=
PUBLIC_ORIGIN=
